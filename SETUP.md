# EEG-Classification

Create GitHub repo and run:
```bash
cd ~/.openclaw/workspace/EEG-Classification
git remote add origin https://github.com/novuxx888/EEG-Classification.git
git push -u origin main
```

Then update cron to push changes automatically.
